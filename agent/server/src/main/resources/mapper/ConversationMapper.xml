<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.clover.mapper.ConversationMapper">

    <!-- 批量插入对话记录 -->
    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO conversation (user_id, session_id, role, content, message_type, tool_calls)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.sessionId}, #{item.role}, #{item.content}, #{item.messageType}, #{item.toolCalls})
        </foreach>
    </insert>

</mapper>
