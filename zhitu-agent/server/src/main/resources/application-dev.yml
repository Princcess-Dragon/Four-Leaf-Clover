clover:
  application:
    name: agent
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      host: localhost
      port: 3306
      database:
      username: 
      password: 
  alioss:
    endpoint: oss-cn-hangzhou.aliyuncs.com
    access-key-id: 
    access-key-secret: 
    bucket-name: test-cloud-storage
  redis:
    host: localhost
    port: 6379
    password: 182764
    database: 10
  servlet:
    multipart:
      max-file-size: 2048MB
      max-request-size: 2048MB

# Spring AI 配置（通义千问兼容 OpenAI API）
spring:
  ai:
    openai:
      # 通义千问 API Key（从 https://dashscope.console.aliyun.com/ 获取）
      api-key: 
      # 通义千问兼容模式 API 地址（注意：不要加 /v1，框架会自动添加）
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          # 模型选择: qwen-turbo / qwen-plus / qwen-max / qwen-long
          model: qwen-turbo
          # 温度参数：控制随机性 (0.0-1.0)，越高越有创造性
          temperature: 0.7
          # 最大 token 数
          max-tokens: 1000
          # 其他可选配置
          # top-p: 0.8  # 核采样参数
          # frequency-penalty: 0.0  # 频率惩罚
          # presence-penalty: 0.0  # 存在惩罚
amap:
  api:
    key: 
